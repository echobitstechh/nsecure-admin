import { ToggleSidebarDirective } from './toggle-sidebar.directive';

describe('ToggleSidebarDirective', () => {
  it('should create an instance', () => {
    const directive = new ToggleSidebarDirective();
    expect(directive).toBeTruthy();
  });
});
